{
  "scripts": {
    "build": "spago build",
    "test": "spago build && jest",
    "upgrade-set": "spago upgrade-set"
  },
  "devDependencies": {
    "jest": "^28.1.0",
    "jest-environment-jsdom": "^28.1.0",
    "purescript": "^0.15.3",
    "spago": "^0.20.9"
  }
}